{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: start",
            "request": "launch",
            "command": "nodemon run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}